kind = "web"
previewPath = "/"
title = "Snapester"
version = "1.0.0"
id = "artifacts/snapester"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20406

[services.development]
run = "pnpm --filter @workspace/snapester run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/snapester", "run", "build" ]
serve = "static"
publicDir = "artifacts/snapester/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20406"
BASE_PATH = "/"
